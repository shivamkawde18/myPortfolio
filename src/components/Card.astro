---
const { cardTitle, cardWidth, cardHeight, cardFooter } = Astro.props;
---

<article class=`${cardWidth} ${cardHeight}`>
  <div
    class=`h-full ${cardWidth} ${cardHeight}  border-2 border-solid border-neutral-border bg-neutral rounded-xl p-4`
  >
    {
      !!cardTitle?.length && (
        <h1 class="mb-4 text-center text-2xl font-semibold tracking-tight">
          {cardTitle}
        </h1>
      )
    }
    <slot />
  </div>
  {
    !!cardFooter?.length && (
      <h1 class="mt-4 text-center text-lg font-semibold tracking-tight">
        {cardFooter}
      </h1>
    )
  }
</article>
